# TT030 (2026-09-12): telnet + ftp for fractal on the LAN; plain text, root has a password.
# No tcpd: /usr/sbin/tcpd is not installed.
telnet	stream	tcp	nowait	root	/usr/sbin/in.telnetd	in.telnetd
ftp	stream	tcp	nowait	root	/usr/sbin/in.ftpd	in.ftpd
